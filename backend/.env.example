DATABASE_URL=sqlite+aiosqlite:///./biking.db
SECRET_KEY=dev-secret-change-me
ADMIN_TOKEN=dev-admin-token
CORS_ORIGINS=http://localhost:5173
FRONTEND_URL=http://localhost:5173
EVENT_START=2026-06-06
EVENT_END=2026-06-28

# Sicherheit
DEBUG=true                         # false in Produktion → dev_token wird nicht zurückgegeben
# FIRST_ADMIN_EMAIL=deine@email.de  # Diese E-Mail bekommt beim ersten Login automatisch Admin
ALTCHA_HMAC_KEY=generate-with-openssl-rand-hex-32   # Altcha Proof-of-Work Schlüssel

# E-Mail via Resend (https://resend.com)

# E-Mail via Scaleway Transactional Email
SMTP_HOST=smtp.tem.scaleway.com
SMTP_PORT=465
SMTP_USER=<scaleway-project-id>
SMTP_PASSWORD=<scaleway-secret-key>
SMTP_FROM=noreply@automail.openzirndorf.de
